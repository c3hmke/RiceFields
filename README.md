# The Rice Fields

Just a collection of files and instructions for ricing various things.

## Fonts
- ***0XProto*** Nerdfont with "half"-ligatures & no samey characters.

## ZSH

 - .zshrc: <br/>
Copy into `~/.config/zsh` then source in `~/.zshrc` to stop idiot colleagues overriding the config when adding outputs to the rc file.

 - .p10k.zsh: <br/>
 PowerLevel10K files. This is old stuff, need to rice some other way but is fine for now.